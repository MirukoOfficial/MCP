How to use them:

Put start-mcp.bat and start-tunnel.bat (or keep-tunnel.bat) inside the same folder as your MCP (where the dist folder is).
Double-click start-mcp.bat first.
Then double-click start-tunnel.bat (or keep-tunnel.bat).
